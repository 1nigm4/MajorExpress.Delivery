<UserControl x:Class="MajorExpress.Delivery.Api.Client.DesktopApp.Components.Editor.OrderEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:app="clr-namespace:MajorExpress.Delivery.Api.Client.DesktopApp"
             xmlns:vm="clr-namespace:MajorExpress.Delivery.Api.Client.DesktopApp.ViewModels.Editor"
             xmlns:local="clr-namespace:MajorExpress.Delivery.Api.Client.DesktopApp.Components.Editor"
             xmlns:fields="clr-namespace:MajorExpress.Delivery.Api.Client.DesktopApp.Components.Fields"
             xmlns:registry="clr-namespace:MajorExpress.Delivery.Api.Client.DesktopApp.Components.Registry"
             d:DataContext="{d:DesignInstance Type=vm:OrderEditorViewModel, IsDesignTimeCreatable=True}"
             x:Name="Root"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <local:EntityEditor>
        <local:EntityEditor.DataView>
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <fields:RegistryField LabelText="Клиент" DisplayValue="{Binding Model.Client.User.FIO}" Registry="{app:Service Type=registry:ClientRegistry}" ModelValue="{Binding Model.Client, Mode=TwoWay}" />
                    <fields:RegistryField Grid.Column="2" LabelText="Исполнитель" DisplayValue="{Binding Model.Courier.User.FIO}" Registry="{app:Service Type=registry:CourierRegistry}" ModelValue="{Binding Model.Courier, Mode=TwoWay}" />
                </Grid>

                <fields:RegistryField Margin="0 10" LabelText="Товар" DisplayValue="{Binding Model.Cargo.Description}" Registry="{app:Service Type=registry:CargoRegistry}" ModelValue="{Binding Model.Cargo, Mode=TwoWay}"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <fields:DateField LabelText="Время погрузки" Text="{Binding Model.PickupTime, Mode=TwoWay}" />
                    <fields:DateField Grid.Column="2" LabelText="Время создания" Text="{Binding Model.CreatedAt, Mode=TwoWay}" />
                </Grid>

                <fields:TextField LabelText="Адрес погрузки" Text="{Binding Model.PickupAddress}" Margin="0 10" />
                <fields:TextField LabelText="Адрес доставки" Text="{Binding Model.DeliveryAddress}" />
                <fields:TextField  LabelText="Причина отмены" Text="{Binding Model.CancelComment}"  Margin="0 10"/>
            </StackPanel>
        </local:EntityEditor.DataView>
        <local:EntityEditor.BottomPanel>
            <StackPanel Orientation="Horizontal">
                <Border CornerRadius="8" Background="#FF2563EB">
                    <Button Content="Передано на выполнение" Command="{Binding ExecuteOrderCommand}" Foreground="{StaticResource AccentColor}" Background="Transparent" BorderThickness="0" Padding="20 4" />
                </Border>
                <Border CornerRadius="8" Background="#FF2563EB" Margin="10 0">
                    <Button Content="Выполнено" Command="{Binding CompleteOrderCommand}" Foreground="{StaticResource AccentColor}" Background="Transparent" BorderThickness="0" Padding="20 4" />
                </Border>
                <Border CornerRadius="8" Background="#FF2563EB">
                    <Button Content="Отменить" Command="{Binding CancelOrderCommand}" Foreground="{StaticResource AccentColor}" Background="Transparent" BorderThickness="0" Padding="20 4" />
                </Border>
            </StackPanel>
        </local:EntityEditor.BottomPanel>
    </local:EntityEditor>
</UserControl>
