<UserControl x:Class="MajorExpress.Delivery.Api.Client.DesktopApp.Components.Fields.RegistryField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:svg="https://github.com/dotnetprojects/SVGImage"
             xmlns:local="clr-namespace:MajorExpress.Delivery.Api.Client.DesktopApp.Components.Fields"
             mc:Ignorable="d"
             x:Name="Root"
             d:DesignHeight="450" d:DesignWidth="400">
    <Grid DataContext="{Binding ElementName=Root}">
        <local:Field LabelText="{Binding LabelText, ElementName=Root}">
            <local:Field.Slot>
                <Grid>
                    <TextBox
                        VerticalContentAlignment="Center"
                        HorizontalAlignment="Stretch"
                        Background="Transparent"
                        BorderThickness="0"
                        FontSize="16"
                        Text="{Binding DisplayValue}"
                        Padding="5 0 5 2" />

                    <Border Grid.Column="1" CornerRadius="8" Background="Transparent" HorizontalAlignment="Stretch">
                        <Button Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Right" Click="OpenRegistry">
                            <svg:SVGImage Height="16" Width="16" Source="/MajorExpress.Delivery.Api.Client.DesktopApp;component/Resources/Assets/search.svg" Margin="6 0" />
                        </Button>
                    </Border>
                </Grid>
            </local:Field.Slot>
        </local:Field>
    </Grid>
</UserControl>
