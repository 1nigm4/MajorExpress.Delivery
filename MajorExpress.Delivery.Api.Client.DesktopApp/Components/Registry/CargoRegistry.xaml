<UserControl x:Class="MajorExpress.Delivery.Api.Client.DesktopApp.Components.Registry.CargoRegistry"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:MajorExpress.Delivery.Api.Client.DesktopApp.ViewModels.Registry"
             xmlns:local="clr-namespace:MajorExpress.Delivery.Api.Client.DesktopApp.Components.Registry"
             d:DataContext="{d:DesignInstance Type=vm:CargoRegistryViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <local:EntityRegistry>
        <local:EntityRegistry.DataView>
            <DataGrid ItemsSource="{Binding Cargos}" MouseDoubleClick="DataGrid_MouseDoubleClick" SelectedItem="{Binding SelectionEntity}" CanUserAddRows="False" IsReadOnly="True" AutoGenerateColumns="False" ColumnHeaderHeight="30">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*" />
                    <DataGridTextColumn Header="Вес" Binding="{Binding Weight}" Width="*" />
                    <DataGridTextColumn Header="Размер" Binding="{Binding Size}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </local:EntityRegistry.DataView>
    </local:EntityRegistry>
</UserControl>
